# ──────────────────────────────────────────────────────────────────────────────
# Cancer Research Assistant — CPU-only dependencies for EC2 / non-GPU servers
# Install with: pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────────────────────

# Use PyTorch's CPU-only wheel index (no CUDA/GPU packages)
--extra-index-url https://download.pytorch.org/whl/cpu

# ── PDF parsing (Step 1) ──
pymupdf>=1.24.0
pdfplumber>=0.11.0

# ── Embeddings (Step 2) ──
sentence-transformers>=3.0.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.26.0

# ── Vector database (Step 3) ──
chromadb>=0.5.0

# ── LLM client (Step 5) ──
groq>=0.11.0

# ── Web UI (Step 6) ──
streamlit>=1.36.0

# ── Configuration ──
python-dotenv>=1.0.0